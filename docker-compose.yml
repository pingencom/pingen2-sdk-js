services:
  js-sdk:
    image: pingen/js-sdk
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NODE_VERSION: ${NODE_VERSION:-24}
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    container_name: js-sdk
    volumes:
      - ./:/usr/src
      - /usr/src/node_modules
